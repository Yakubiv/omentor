= render 'tabs'

%div{ class: "max-w-7xl mx-auto px-4 sm:px-6 md:px-8" }
  .card
    .card-header.bline
      = t('tutors.settings.avatar_header')
      .card-options
        %a.card-options-fullscreen{"data-toggle" => "card-fullscreen", :href => "#"}
          %i.fe.fe-maximize
    = form_with(model: current_student_profile, url: students_settings_path, method: :put) do |f|
      .card-body
        = render 'students/details/fields/photo', f: f
      .card-footer.text-right
        = f.submit t('tutors.details.save').html_safe, class: 'btn btn-primary'