document.getElementById(
  'heart-favorite-container-'+ <%= @student_profile.id %>
).innerHTML =
  "<%= escape_javascript(render partial: 'tutors/students/favorites/remove_favorite', locals: { student: @student_profile }) %>"
